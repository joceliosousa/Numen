projection;
strict ( 2 );
use draft;
use side effects;

define behavior for ZC_MAT_RME alias MaterialMRE
use etag

{
  use create;
  use update;
  use delete;

  use action copy;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
}
